# 🏗️ Architecture de l'Application

## 📊 Vue d'Ensemble

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND                             │
│                    (Next.js App Router)                     │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │  Login   │  │ Register │  │Dashboard │  │  Books   │  │
│  │  Page    │  │   Page   │  │   Page   │  │  Pages   │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                                                             │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ HTTP Requests
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                     MIDDLEWARE                              │
│                  (middleware.ts)                            │
│                                                             │
│  ✅ Check Authentication                                    │
│  ✅ Protect API Routes                                      │
│  ✅ Redirect Logic                                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                    API ROUTES                               │
│                  (app/api/...)                              │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  /api/auth/register          (POST)                 │  │
│  │  /api/auth/[...nextauth]     (GET, POST)            │  │
│  ├──────────────────────────────────────────────────────┤  │
│  │  /api/books                  (GET, POST)            │  │
│  │  /api/books/[id]             (GET, PUT, DELETE)     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ✅ Validation (Zod)                                        │
│  ✅ Error Handling                                          │
│  ✅ User Isolation                                          │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                   NEXTAUTH v5                               │
│                    (auth.ts)                                │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Credentials Provider                                │  │
│  │  - Email/Password Authentication                    │  │
│  │  - bcrypt Password Hashing                          │  │
│  │  - JWT Strategy                                      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  Future: Google OAuth Provider                             │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                   PRISMA ORM                                │
│                  (lib/prisma.ts)                            │
│                                                             │
│  ✅ Singleton Client                                        │
│  ✅ Type-Safe Queries                                       │
│  ✅ Auto-Generated Types                                    │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                  PostgreSQL DATABASE                        │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  User                                                │  │
│  │  - id, name, email, password, createdAt              │  │
│  └──────────────────────────────────────────────────────┘  │
│                         │                                   │
│                         │ 1:N                               │
│                         ▼                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Book                                                │  │
│  │  - id, userId, title, author, rating, comment        │  │
│  │  - startDate, endDate, createdAt, updatedAt          │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Account (NextAuth)                                  │  │
│  │  - For OAuth providers                               │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Session (NextAuth)                                  │  │
│  │  - For session management                            │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Flux de Données

### 1. Inscription d'un Utilisateur

```
User Input (name, email, password)
    │
    ▼
POST /api/auth/register
    │
    ├─► Validation (Zod)
    │
    ├─► Check if user exists (Prisma)
    │
    ├─► Hash password (bcrypt)
    │
    ├─► Create user (Prisma)
    │
    └─► Return user data
```

### 2. Connexion

```
User Input (email, password)
    │
    ▼
NextAuth Credentials Provider
    │
    ├─► Validation (Zod)
    │
    ├─► Find user by email (Prisma)
    │
    ├─► Compare passwords (bcrypt)
    │
    ├─► Generate JWT token
    │
    └─► Create session
```

### 3. Création d'un Livre

```
User Input (title, author, rating, etc.)
    │
    ▼
Middleware (Check Authentication)
    │
    ▼
POST /api/books
    │
    ├─► Get user from session
    │
    ├─► Validation (Zod)
    │
    ├─► Create book with userId (Prisma)
    │
    └─► Return book data
```

### 4. Liste des Livres

```
User Request
    │
    ▼
Middleware (Check Authentication)
    │
    ▼
GET /api/books
    │
    ├─► Get user from session
    │
    ├─► Find books by userId (Prisma)
    │
    └─► Return books array
```

## 🔒 Sécurité

### Couches de Sécurité

```
┌─────────────────────────────────────────────────────────────┐
│  1. MIDDLEWARE                                              │
│     ✅ Check if user is authenticated                       │
│     ✅ Protect API routes                                   │
│     ✅ Redirect unauthenticated users                       │
└─────────────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  2. API ROUTE VALIDATION                                    │
│     ✅ Validate request body (Zod)                          │
│     ✅ Check user session                                   │
│     ✅ Verify user owns the resource                        │
└─────────────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  3. DATABASE CONSTRAINTS                                    │
│     ✅ Foreign key constraints                              │
│     ✅ Unique constraints (email)                           │
│     ✅ Cascade delete                                       │
└─────────────────────────────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│  4. DATA ENCRYPTION                                         │
│     ✅ Password hashing (bcrypt, 10 rounds)                 │
│     ✅ JWT tokens (signed)                                  │
│     ✅ Environment variables (.env)                         │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Modules et Responsabilités

### Backend Core

| Module | Responsabilité | Fichier |
|--------|---------------|---------|
| **Prisma Client** | Connexion DB, Queries | `lib/prisma.ts` |
| **NextAuth Config** | Authentification | `auth.ts` |
| **Middleware** | Protection routes | `middleware.ts` |
| **Types** | TypeScript types | `types/next-auth.d.ts` |

### API Routes

| Route | Responsabilité | Fichier |
|-------|---------------|---------|
| **Register** | Inscription utilisateur | `app/api/auth/register/route.ts` |
| **NextAuth** | Login/Logout | `app/api/auth/[...nextauth]/route.ts` |
| **Books List** | GET/POST livres | `app/api/books/route.ts` |
| **Book Detail** | GET/PUT/DELETE livre | `app/api/books/[id]/route.ts` |

### Database

| Table | Responsabilité | Relations |
|-------|---------------|-----------|
| **User** | Utilisateurs | → books, accounts, sessions |
| **Book** | Livres | ← user |
| **Account** | OAuth providers | ← user |
| **Session** | Sessions NextAuth | ← user |
| **VerificationToken** | Email verification | - |

## 🎯 Patterns Utilisés

### 1. Singleton Pattern
```typescript
// lib/prisma.ts
export const prisma = globalForPrisma.prisma ?? new PrismaClient()
```
**Pourquoi ?** Éviter les connexions multiples à la base de données.

### 2. Middleware Pattern
```typescript
// middleware.ts
export default auth((req) => {
  // Protection des routes
})
```
**Pourquoi ?** Centraliser la logique d'authentification.

### 3. Repository Pattern (via Prisma)
```typescript
// Abstraction de la base de données
await prisma.book.findMany({ where: { userId } })
```
**Pourquoi ?** Type-safety et abstraction de la DB.

### 4. Validation Pattern (Zod)
```typescript
const schema = z.object({ title: z.string().min(1) })
const validated = schema.safeParse(data)
```
**Pourquoi ?** Validation type-safe des données.

## 🚀 Performance

### Optimisations Implémentées

1. **Index sur userId** dans la table Book
   ```prisma
   @@index([userId])
   ```

2. **Singleton Prisma Client**
   - Évite les connexions multiples
   - Réutilise la connexion existante

3. **JWT Strategy**
   - Pas de requête DB à chaque requête
   - Session stockée dans le token

### Optimisations Futures

- [ ] Pagination sur GET /api/books
- [ ] Cache avec Redis
- [ ] CDN pour les images
- [ ] Lazy loading des livres
- [ ] Infinite scroll

## 📈 Scalabilité

### Actuellement Supporté

- ✅ Multi-utilisateurs
- ✅ Isolation des données
- ✅ Transactions automatiques (Prisma)
- ✅ Type-safety complète

### Pour Scaler

1. **Horizontal Scaling**
   - Déployer sur Vercel (serverless)
   - Base de données managée (Supabase, PlanetScale)

2. **Vertical Scaling**
   - Augmenter les ressources DB
   - Ajouter des indexes

3. **Caching**
   - Redis pour les sessions
   - CDN pour les assets

---

**Architecture solide et prête pour la production ! 🎉**
